# SoDam-Reverse-Eng

# 분석 결과(보고서) — 로컬 전용, 유출 방지
.sodam-re/

# 의존성 / 로그 / OS
node_modules/
*.log
.DS_Store
Thumbs.db

# 환경설정 (시크릿 추적 금지)
.env
.env.*

# 내부 기획 문서 (GitHub 푸시 금지)
.PRD/

# 내부 감사 로그 — 미적용 보안패치·우회기법 재현법을 구체적으로 기록하므로 공개저장소 추적 금지
# (2026-08-17: repo가 PUBLIC이고 CHECKPOINT.md가 feat/m5-readiness 브랜치에 공개 노출된 것을 확인 후 조치.
#  파일 자체는 로컬에 그대로 유지되어 세션 인계 기능에는 영향 없음 — git 추적에서만 제외)
CHECKPOINT.md

# PDF 중간 생성 HTML 임시 파일 (아래 문서 HTML 2종은 영구 산출물이라 예외로 추적)
/*.html
!/README.html
!/README.en.html

# 로컬 전용 플러그인 설정 및 캐시
.omc/
.plugin-config/
.todos-report.md/
.complexity-log.md

# AI 페어프로그래밍 세션 리포트 (자동 생성, 로컬 전용)
.pair-programming-session.md

# 외부 하네스/훅이 남기는 실패 추적 로그 (이 프로젝트 소스 아님, 2026-08-20 발견)
.failure-tracker.jsonl
